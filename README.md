# vless-xtls-vision-installer
A one-file bash bootstrap for deploying VLESS over REALITY (xtls-rprx-vision). Designed for fresh Linux hosts, the script automates installation, configuration, and startup of the service without requiring external configuration management tools.
